const a= 10;
a=20;

//TypeError: Assignment to constant variable. 
//declarations must be initialized
console.log("a",a);

//const a;

// scope : area till which a given variable is available

// var --> function 
//let/ const --> block scope

//scop chain : when a variable isnt available inside the current scope then we will look outside the outer scope till u are not able to find that variable

